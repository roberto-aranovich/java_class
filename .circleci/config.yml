version: 2
jobs:
  build:
    docker:
     - image: alpine:3.7
    steps:
      - checkout
      - run: <command>
  test:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run: <command>
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test